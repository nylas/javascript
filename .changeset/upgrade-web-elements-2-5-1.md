---
"@nylas/react": patch
---

Upgrade @nylas/web-elements dependency from ^2.2.4 to 2.5.1

- Add new `NylasNotetakerConfig` component with `onValueChanged` and `onNotetakerError` events
- Re-enable `NylasOnlySpecificTimeAvailability` component (previously commented out as unavailable)
- No breaking changes to existing component types or interfaces
